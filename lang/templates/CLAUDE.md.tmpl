# {{name}}

Synoema project. See AGENTS.md for full language reference.

## Commands

```
synoema run src/main.sno    -- run (interpreter)
synoema jit src/main.sno    -- run (JIT, native speed)
synoema test tests/         -- test
synoema doc src/            -- docs
synoema fmt src/            -- format
```

## MCP Server

Configured in `.claude/settings.json`. Tools: eval, typecheck, run.
