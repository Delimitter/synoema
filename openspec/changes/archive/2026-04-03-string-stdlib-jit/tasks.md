# Tasks: String Stdlib in JIT

- [x] Implement `synoema_str_slice` in runtime.rs + register + declare + test
- [x] Implement `synoema_str_find` in runtime.rs + register + declare + test
- [x] Implement `synoema_str_starts_with` in runtime.rs + register + declare + test
- [x] Implement `synoema_str_trim` in runtime.rs + register + declare + test
- [x] Implement `synoema_str_len` in runtime.rs + register + declare + test
- [x] Implement `synoema_json_escape` in runtime.rs + register + declare + test
- [x] Run full test suite — 0 failures, 0 warnings (705 tests passed)
