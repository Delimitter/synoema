# Tasks: VSCode Extension Install Script

- [x] Создать `vscode-extension/install.sh` с полным пайплайном (check deps → npm install → build → package → install → cleanup)
- [x] Сделать скрипт исполняемым (`chmod +x`)
- [x] Исправить `.vscodeignore` (убрать `dist` из exclude — он нужен в пакете)
- [x] Убрать ссылку на несуществующую иконку из `package.json`
- [x] Проверить что скрипт работает (запустить)
