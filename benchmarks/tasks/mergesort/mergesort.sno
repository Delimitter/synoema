-- SPDX-License-Identifier: MIT-0

merge xs ys =
  ? xs == [] -> ys
  : ? ys == [] -> xs
  : ? head xs <= head ys -> (head xs : merge (tail xs) ys)
  : (head ys : merge xs (tail ys))

msort xs =
  n = length xs / 2
  ? n == 0 -> xs
  : merge (msort (take n xs)) (msort (drop n xs))

main = msort [5 3 8 1 9 2 7 4 6]
